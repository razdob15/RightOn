import { Typography } from '@mui/material';

export const DebugComp = () => {
  return (
    <div>
      <Typography variant="h4" component="h1" gutterBottom>
        שלום! זהו רכיב DebugComp
      </Typography>
      <Typography variant="body1">האפליקציה פועלת כעת בעברית</Typography>
    </div>
  );
};
